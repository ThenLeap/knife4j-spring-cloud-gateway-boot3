server:
  port: 9002
  servlet:
    context-path: /user
spring:
  application:
    name: user-service
  cloud:
    nacos:
      server-addr: localhost:8848

knife4j:
  enable: true
  setting:
    language: zh_cn
